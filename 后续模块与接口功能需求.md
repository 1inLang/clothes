# 服装设计业务流程管理系统——后续模块与接口功能需求

> 基线日期：2026-07-29  
> 当前进度：登录注册、Sa-Token 会话、用户与多角色关联、角色权限、设计项目、项目成员、设计任务、设计稿与附件、审核与返修、工作台与统计、站内通知及对应前端接口接入已经完成。

## 1. 下一步优先开发什么

下一模块建议开发：**审计与登录日志**。

原因：

1. 工作台和站内通知已经形成日常协作入口，核心业务事件能够主动触达相关人员。
2. 审计日志可以记录用户、权限、项目、任务、设计稿、审核和验收写操作。
3. 登录日志和操作日志完成后，系统将具备完整的安全追踪与问题定位能力。

推荐按照以下顺序继续：

1. 设计项目
2. 项目成员
3. 设计任务与状态流转
4. 设计稿、附件与版本
5. 审核与返修
6. 工作台与统计
7. 站内通知
8. 操作日志与登录日志

---

## 2. 通用接口约定

### 2.1 基础路径

```text
/api
```

### 2.2 统一响应

继续使用现有结构：

```json
{
  "code": 0,
  "data": {},
  "message": "ok"
}
```

建议语义：

| 场景 | HTTP 状态 | 业务码 |
| --- | ---: | ---: |
| 成功 | 200 | 0 |
| 参数错误 | 400 | 40000 |
| 未登录 | 401 | 40100 |
| 无权限 | 403 | 40101 |
| 数据不存在 | 404 | 40400 |
| 状态冲突、重复操作 | 409 | 40900 |
| 系统异常 | 500 | 50000 |

### 2.3 ID 传输

后端使用雪花 ID，所有 ID 在 JSON 中必须使用字符串传输：

```json
{
  "id": "2018576037261094914"
}
```

不要直接向 JavaScript 返回 64 位数值，否则会产生精度丢失。

### 2.4 分页请求

```json
{
  "current": 1,
  "pageSize": 10,
  "sortField": "createTime",
  "sortOrder": "descend"
}
```

分页响应：

```json
{
  "records": [],
  "total": 0,
  "current": 1,
  "size": 10,
  "pages": 0
}
```

### 2.5 数据规则

- 所有业务表使用逻辑删除。
- 所有查询默认过滤已删除数据。
- 排序字段必须使用白名单映射，不能直接拼接客户端字段。
- 写操作必须记录创建人、更新人和操作时间。
- 前端隐藏按钮不能替代后端权限校验。
- 对象不存在和无权访问时，不能泄露受限业务数据是否存在。

---

## 3. 角色与权限管理模块

### 3.1 目标

- 维护角色基础信息。
- 维护系统权限码。
- 为角色分配权限。
- 为用户分配一个或多个角色。
- `StpInterface` 从数据库或缓存读取角色和权限。
- 角色和授权变化后及时刷新权限。

### 3.2 数据表

#### `sys_role`

| 字段 | 说明 |
| --- | --- |
| id | 角色 ID |
| role_name | 角色名称 |
| role_code | 角色标识，如 `designer` |
| description | 角色说明 |
| status | 0 停用、1 正常 |
| is_system | 是否系统内置角色 |
| create_time / update_time | 时间字段 |
| is_delete | 逻辑删除 |

#### `sys_permission`

| 字段 | 说明 |
| --- | --- |
| id | 权限 ID |
| permission_name | 权限名称 |
| permission_code | 权限码，如 `project:create` |
| module | 所属模块 |
| description | 权限说明 |
| status | 状态 |

#### `sys_user_role`

```text
id、user_id、role_id、create_time
```

用户和角色为多对多关系。

#### `sys_role_permission`

```text
id、role_id、permission_id、create_time
```

角色和权限为多对多关系。

### 3.3 接口需求

#### 角色分页查询

```http
POST /api/role/list/page
```

权限：

```text
role:manage
```

请求：

```json
{
  "current": 1,
  "pageSize": 10,
  "roleName": "",
  "roleCode": "",
  "status": 1
}
```

#### 角色详情

```http
GET /api/role/get?id={roleId}
```

返回角色信息以及已分配权限 ID。

#### 创建角色

```http
POST /api/role/add
```

```json
{
  "roleName": "版型审核人",
  "roleCode": "pattern_reviewer",
  "description": "负责版型与工艺审核",
  "permissionIds": ["权限ID1", "权限ID2"]
}
```

要求：

- `roleCode` 唯一。
- 只允许字母、数字、下划线。
- 创建角色和分配权限应在同一事务中完成。

#### 更新角色

```http
POST /api/role/update
```

系统内置角色可以限制修改 `roleCode`。

#### 删除角色

```http
POST /api/role/delete
```

删除前必须确认：

- 角色不是系统内置角色。
- 没有用户仍然引用该角色。

#### 启用或停用角色

```http
POST /api/role/change-status
```

#### 权限树

```http
GET /api/permission/tree
```

按模块返回权限：

```json
[
  {
    "module": "项目",
    "permissions": [
      {
        "id": "权限ID",
        "permissionCode": "project:create",
        "permissionName": "创建项目"
      }
    ]
  }
]
```

#### 为用户分配角色

```http
POST /api/user/assign-roles
```

```json
{
  "userId": "用户ID",
  "roleIds": ["角色ID1", "角色ID2"]
}
```

#### 查询用户角色

```http
GET /api/user/roles?userId={userId}
```

### 3.4 Sa-Token 接入要求

`StpInterface` 需要实现：

```java
List<String> getRoleList(Object loginId, String loginType);
List<String> getPermissionList(Object loginId, String loginType);
```

数据来源：

```text
用户 → sys_user_role → sys_role → sys_role_permission → sys_permission
```

要求：

- 不再使用 `switch` 将角色和权限写死。
- 停用角色不参与权限计算。
- 账号或角色被停用后，权限立即或在可控时间内失效。
- 授权变更记录变更人、变更前角色、变更后角色和变更时间。
- 可使用 Sa-Token 踢用户下线，使新权限立即生效。

### 3.5 验收条件

- 管理员可以创建、编辑、停用和删除非内置角色。
- 管理员可以为角色勾选权限。
- 管理员可以为用户分配多个角色。
- 用户最终权限为全部有效角色权限的并集。
- 无权限用户直接调用接口返回 403。
- 角色变更后旧权限不再长期生效。

---

## 4. 设计项目模块

### 4.1 核心字段

```text
id、project_code、project_name、category、season、style、
target_audience、requirement、manager_id、acceptor_id、priority、status、
plan_start_date、plan_end_date、progress、version
```

### 4.2 项目状态

```text
草稿 → 已立项 → 设计中 → 验收中 → 已完成
  └──────────────→ 已取消
```

### 4.3 接口

| 接口 | 方法 | 权限 |
| --- | --- | --- |
| `/api/project/list/page` | POST | `project:view` |
| `/api/project/get` | GET | `project:view` + 数据权限 |
| `/api/project/add` | POST | `project:create` |
| `/api/project/update` | POST | `project:update` |
| `/api/project/submit` | POST | `project:update` |
| `/api/project/start-design` | POST | `project:update` |
| `/api/project/submit-acceptance` | POST | `project:submit_acceptance` |
| `/api/project/accept` | POST | `project:acceptance:review` |
| `/api/project/reject-acceptance` | POST | `project:acceptance:review` |
| `/api/project/acceptance/history` | GET | `project:view` + 数据权限 |
| `/api/project/acceptance/candidates` | GET | `project:update` |
| `/api/project/cancel` | POST | `project:close` |
| `/api/project/progress` | GET | `project:view` |

### 4.4 项目查询条件

- 项目编号
- 项目名称
- 状态
- 项目负责人
- 品类
- 季节
- 优先级
- 计划时间范围

### 4.5 状态规则

- 草稿提交立项前必须填写名称、品类和计划日期。
- 已立项进入设计中前至少配置一名项目成员。
- 设计中进入验收前，全部必需任务必须完成。
- 只有项目负责人可以提交验收，且提交前必须指定与负责人不同的项目验收人。
- 只有项目 `acceptor_id` 指定的验收人可以通过或退回验收；系统管理员不会自动取得业务验收资格。
- 验收退回必须填写原因。
- 每次通过或退回都会生成不可覆盖的 `project_acceptance_record`，并使用请求号保证幂等。
- 已完成或已取消项目只读。

### 4.6 当前实现说明

- 本模块后端接口、Sa-Token 权限注解、乐观锁、验收职责分离和 Vue 页面已经完成。
- 项目负责人提交验收时校验实际任务：至少存在一项未取消任务，且所有未取消任务均已完成。
- Vue 项目页面支持指定验收人、按当前身份显示提交或验收按钮，并可查看项目验收历史。

---

## 5. 项目成员模块

### 5.1 数据表

`project_member`

```text
id、project_id、user_id、project_role、join_time、is_delete
```

项目内角色示例：

- 项目经理
- 设计师
- 审核人
- 只读成员

### 5.2 接口

| 接口 | 方法 | 权限 |
| --- | --- | --- |
| `/api/project/member/list` | GET | `project:view` |
| `/api/project/member/add` | POST | `project:member` |
| `/api/project/member/update` | POST | `project:member` |
| `/api/project/member/remove` | POST | `project:member` |
| `/api/project/member/candidates` | GET | `project:member` |

### 5.3 数据权限

- 非项目成员不能查看项目详情。
- 项目成员被移除后立即失去项目访问权限。
- 历史操作记录仍需保留成员当时的信息。

### 5.4 当前实现说明

- 成员表、成员角色、候选人查询、增改移除接口和 Vue 成员管理界面已经完成。
- 新建项目会自动将负责人写入成员表；更换负责人时会自动同步项目经理成员。
- 管理员可访问全部项目，其他用户仅能访问自己负责或作为有效成员加入的项目。
- 成员采用逻辑删除，移除后立即失去项目访问权限，同时保留历史关系记录。

---

## 6. 设计任务模块

### 6.1 核心字段

```text
id、project_id、task_code、task_name、requirement、assignee_id、
reviewer_id、priority、status、deadline、progress、version
```

### 6.2 任务状态

```text
待分配 → 待领取 → 进行中 → 待审核 → 已完成
                    ↑         │
                    └─ 退回修改
```

### 6.3 接口

| 接口 | 方法 | 权限 |
| --- | --- | --- |
| `/api/task/list/page` | POST | `task:view` |
| `/api/task/my/page` | POST | `task:view` |
| `/api/task/get` | GET | `task:view` + 数据权限 |
| `/api/task/add` | POST | `task:create` |
| `/api/task/update` | POST | `task:create` |
| `/api/task/assign` | POST | `task:assign` |
| `/api/task/accept` | POST | `task:submit` |
| `/api/task/update-progress` | POST | `task:submit` |
| `/api/task/submit-review` | POST | `task:submit` |
| `/api/task/cancel` | POST | `task:assign` |

### 6.4 关键规则

- 只有被指派设计师可以接受任务。
- 提交审核前至少存在一个有效设计稿版本。
- 任务负责人只能操作本人任务。
- 任务审核人不能由前端随意替换。
- 所有状态转换使用统一状态机服务校验。
- 状态更新使用乐观锁，防止重复提交。

### 6.5 当前实现说明

- 任务表、分页与我的任务、详情、创建、编辑、分派、领取、进度更新、提交审核和取消接口已经完成。
- 任务负责人和审核人必须来自当前项目成员，且两者不能是同一人。
- 项目经理负责创建和分派，任务负责人只能操作本人任务，所有更新均携带乐观锁版本号。
- 项目提交验收已经改为校验实际任务：至少存在一项任务，且所有未取消任务均已完成。
- 设计稿模块尚未落地，当前暂以任务进度达到 `100%` 作为提交审核的代理条件；设计稿模块完成后替换为有效版本校验。

---

## 7. 设计稿与附件模块

### 7.1 数据表

`design_file`

```text
id、project_id、task_id、file_name、storage_key、
file_type、mime_type、file_size、version_no、version_note、
uploader_id、create_time、is_delete
```

### 7.2 接口

| 接口 | 方法 | 权限 |
| --- | --- | --- |
| `/api/file/upload` | POST multipart | `design:upload` |
| `/api/file/list` | GET | `design:view` |
| `/api/file/version/list` | GET | `design:view` |
| `/api/file/get` | GET | `design:view` |
| `/api/file/download` | GET | `design:download` |
| `/api/file/delete` | POST | `design:delete` |

### 7.3 文件规则

- 每次提交创建新版本，不能覆盖历史文件。
- 保存文件前校验扩展名、MIME 和大小。
- 下载前重新验证项目数据权限。
- 已进入审核流程的版本不能删除。
- 文件保存失败时不能留下空版本记录。
- 存储层抽象为接口，支持本地文件和 MinIO。

### 7.4 当前实现说明

- 文件元数据表、可配置本地存储、上传、列表、版本查询、详情、鉴权下载和逻辑删除接口已经完成。
- 支持设计稿、参考资料和其他附件，单文件上限为 `50MB`，上传时校验扩展名、MIME 和文件头特征。
- 存储键由服务端按年月和随机 UUID 生成，客户端文件名不会参与磁盘路径解析。
- 每次上传自动生成递增版本号；任务提交审核时自动锁定最新有效设计稿并写入 `submitted_file_id`。
- 已提交审核的版本禁止删除；普通删除仅逻辑删除元数据并保留物理文件，以便审计和误删恢复。
- 当前使用本地目录 `./data/uploads`，存储访问已封装为接口，后续可新增 MinIO 实现。

---

## 8. 审核与返修模块

### 8.1 数据表

`review_record`

```text
id、task_id、file_id、version_no、reviewer_id、result、
opinion、create_time、request_no
```

### 8.2 接口

| 接口 | 方法 | 权限 |
| --- | --- | --- |
| `/api/review/pending/page` | POST | `task:review` |
| `/api/review/detail` | GET | `task:review` |
| `/api/review/history` | GET | `task:view` |
| `/api/review/approve` | POST | `task:review` |
| `/api/review/reject` | POST | `task:review` |

审核请求：

```json
{
  "taskId": "任务ID",
  "taskVersion": 4,
  "versionNo": 3,
  "opinion": "请调整袖口比例",
  "requestNo": "客户端幂等号"
}
```

### 8.3 关键规则

- 只有任务指定审核人且具有 `task:review` 权限才能审核。
- 只有“待审核”任务可以审核。
- 退回修改时审核意见必填。
- 每次审核生成不可覆盖记录。
- 同一待审核状态只能有一个审核结果成功。
- 审核通过后任务进入已完成。
- 审核退回后任务进入“退回修改”状态，设计师修改后可再次提交审核。

### 8.4 当前实现状态

- 待审核列表按当前登录用户的 `reviewer_id` 过滤，未被指定为审核人的用户无法查看或操作审核详情。
- 审核详情返回当前锁定设计稿版本和不可覆盖的历史审核记录；图片稿件支持页面预览，其他文件支持鉴权下载。
- 审核通过将任务更新为“已完成”；审核退回将任务更新为“退回修改”，并保存退回原因。
- 使用任务 `version` 乐观锁保证同一待审核状态只有一个审核结果成功，使用唯一 `request_no` 保证重复请求幂等。
- Vue 审核中心已经接入待审核列表、审核详情、审核历史、通过和退回接口，不再使用静态 Mock 数据（演示模式除外）。
- 审核结果站内通知将在第 10 节“站内通知模块”开发时接入。

---

## 9. 工作台与统计模块

### 9.1 接口

```http
GET /api/dashboard/summary
GET /api/dashboard/my-todos
GET /api/dashboard/project-status
GET /api/dashboard/task-completion
GET /api/dashboard/recent-activities
```

### 9.2 汇总指标

- 我的待办
- 待我审核
- 三天内到期
- 已逾期
- 本人负责项目状态分布
- 任务完成率
- 最近活动

### 9.3 数据权限

所有统计必须基于当前用户的项目成员关系和功能权限计算，不能先全量统计再由前端过滤。

### 9.4 当前实现状态

- 五个工作台接口及 `dashboard:view` 权限已经完成，所有系统业务角色默认获得工作台查看权限。
- 我的待办包含本人负责的待领取、进行中和退回修改任务，以及待本人审核的设计稿和待本人验收的项目。
- 临期与逾期数量只基于个人待办计算；项目状态、任务完成率和最近动态只统计当前用户有权访问的项目。
- 最近动态来自真实设计稿上传、设计审核和项目验收记录，不使用前端静态数据（演示模式除外）。
- Vue 工作台已经接入汇总卡片、重点待办、项目进度、任务完成率、项目状态分布和最近动态。

---

## 10. 站内通知模块

### 10.1 通知场景

- 任务被分派
- 任务提交审核
- 审核通过
- 审核退回
- 任务即将到期
- 项目成员变化

### 10.2 接口

| 接口 | 方法 |
| --- | --- |
| `/api/notification/list/page` | POST |
| `/api/notification/unread-count` | GET |
| `/api/notification/read` | POST |
| `/api/notification/read-all` | POST |

通知需要包含：

```text
标题、内容、业务对象类型、业务对象 ID、跳转地址、是否已读、创建时间
```

### 10.3 当前实现状态

- 已完成 `system_notification` 持久化表、通知查询、未读数、单条已读和全部已读接口。
- 通知严格按 `receiver_id` 查询和更新，用户不能读取或修改其他用户的通知。
- 任务分派时通知任务负责人和审核人；设计稿提交审核时通知指定审核人；审核结果通知设计师。
- 项目提交验收时通知指定验收人；验收通过或退回时通知项目负责人；项目成员新增、角色调整和移除时通知对应成员。
- 用户打开系统或通知中心时，会为本人三天内到期的未完成任务生成临期提醒；业务幂等键避免同一期限重复提醒。
- Vue 通知中心已经接入真实分页、类型筛选、只看未读、单条已读、全部已读、详情跳转和侧栏实时未读数（演示模式除外）。

---

## 11（不使用）. 审计与登录日志模块

### 11.1 日志类型

- 登录成功
- 登录失败
- 主动退出
- 账号锁定
- 管理员强制下线
- 用户、角色和授权变化
- 项目、任务、设计稿和审核写操作

### 11.2 接口

| 接口 | 方法 | 权限 |
| --- | --- | --- |
| `/api/audit/list/page` | POST | `audit:view` |
| `/api/audit/get` | GET | `audit:view` |
| `/api/audit/export` | POST | `audit:export` |
| `/api/login-log/list/page` | POST | `audit:view` |

### 11.3 日志字段

```text
操作者、动作、对象类型、对象 ID、对象编号、请求参数摘要、
执行结果、错误信息、来源 IP、User-Agent、请求时间、耗时
```

普通管理员不得修改或删除审计日志。导出日志的操作本身也需要记录。

---

## 12. 推荐开发迭代

### 迭代一：权限基础

- 角色 CRUD
- 权限树
- 用户角色分配
- 数据库版 `StpInterface`
- 授权变更日志

验收目标：管理员可通过页面动态配置角色，接口权限实时生效。

### 迭代二：项目基础

- 项目 CRUD
- 项目状态机
- 项目成员
- 项目数据权限

验收目标：项目经理可以建立项目并维护成员，非成员不能访问。

### 迭代三：任务闭环

- 任务创建、分派和接受
- 设计稿上传与版本
- 提交审核
- 审核通过和退回

验收目标：完成一次“分派 → 设计 → 提交 → 审核 → 完成/返修”闭环。

### 迭代四：运营与交付

- 工作台
- 通知
- 审计日志
- 统计
- 文件存储配置
- 部署与初始化数据

验收目标：管理员和业务人员能够在完整权限边界下日常使用系统。

---

## 13. 开发完成定义

每个模块完成时至少满足：

- Controller、Service、Mapper 和数据库迁移齐全。
- 接口包含参数校验、权限校验、数据权限和状态校验。
- 列表支持分页和必要筛选。
- 64 位 ID 使用字符串传输。
- 危险操作有明确错误信息，不返回假成功。
- 写操作有事务与审计记录。
- 前端页面已接入真实接口，不继续依赖演示数据。
- 包含正常、无权限、数据不存在、重复提交和并发状态变化测试。
- Knife4j/OpenAPI 文档能够正确展示请求和响应模型。
